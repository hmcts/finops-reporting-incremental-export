name: Cost Management API
on:
  workflow_dispatch:
  schedule:
    - cron: '0 20 * * 1-5' # Every weekday at 8pm UTC
permissions:
  id-token: write
# set vars for script here 
env: 
  subscription_name: DTS-SHAREDSERVICES-SBOX
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Az CLI login'
        uses: azure/login@v1
        with:
          client-id: c9f9bf69-1285-4857-8979-7f644e922918 # DTS Enterprise reporting
          tenant-id: 531ff96d-0ae9-462a-8d2d-bec7c0b42082 # HMCTS.NET
          allow-no-subscriptions: true
      - name: Cost Management API
        run: ./components/bash-scripts/balance_summary_export.sh